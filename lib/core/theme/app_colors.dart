import 'package:flutter/material.dart';

class AppColors {
  // Donghua aesthetically inspired dark theme palette
  static const Color background = Color(0xFF0D1117);
  static const Color surface = Color(0xFF161B22);
  
  // Accents (Jade Green, Cyan, Ethereal Gold)
  static const Color jade = Color(0xFF00E676);
  static const Color cyan = Color(0xFF00B8D4);
  static const Color etherealGold = Color(0xFFFFD54F);
  
  // Typography colors
  static const Color textPrimary = Color(0xFFE6EDF3);
  static const Color textSecondary = Color(0xFF8B949E);
  static const Color textAccent = jade;
}
